name: 04_dotnetpublish_01_proxy
on:
    push:
        branches:
            - "main"
            - "Develop"
    # pull_request:
    #     branches:
    #         - "main"
jobs:
    call-Build-release-workflow-from-store-publish:
        uses: "./.github/workflows/04_dotnetpublish_00_buildRelease.yml"
    build:
        runs-on: ubuntu-latest
        needs: call-Build-release-workflow-from-store-publish
        steps:
            - name: Download proxy artifact
              uses: actions/download-artifact@v4
              with:
                  name: proxy-artifact
                  path: artifacts
            - name: Download react artifact
              uses: actions/download-artifact@v4
              with:
                  name: store-artifact
                  path: wwwroot
            - name: Display structure of downloaded files
              run: ls -R
